# Optional overrides. Keys stay in the repo-root .env, not here.
#   cp ../.env.example ../.env

DATABASE_URL=postgresql://sentinel:sentinel@localhost:5433/sentinel
SENTINEL_MCP_URL=http://127.0.0.1:8090
